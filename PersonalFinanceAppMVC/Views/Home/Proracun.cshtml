@model IEnumerable<MyProracun>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proracun</title>
    <link rel="stylesheet" href="~/css/Proracun.css" />
</head>
<body>

    <h2>Proracun</h2>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dopušteni Budžet</th>
                <th>Mjesec</th>
                <th>Odjel</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var budget in Model)
            {
                <tr>
                    <td>@budget.Id</td>
                    <td>@budget.budzet</td>
                    <td>@budget.Month</td>
                    <td>@budget.Department</td>
                </tr>
            }
        </tbody>
    </table>

    <div class="form-container">
        <h2>Add Proracun</h2>
        <form id="proracunForm" action="submit" method="post">
            <div class="form-group">
                <label for="allowedBudget">Allowed Budget:</label>
                <input type="text" id="allowedBudget" name="allowedBudget" required>
            </div>
            <div class="form-group">
                <label for="month">Month:</label>
                <select id="month" name="month" required>
                    <option value="">-- Select Month --</option>
                    @foreach (var month in Enum.GetNames(typeof(Month)))
                    {
                        <option value="@month">@month</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label for="department">Department:</label>
                <select id="department" name="department" required>
                    <option value="">-- Select Department --</option>
                    <option value="Department1">Finance</option>
                    <option value="Department2">Videogames</option>
                    <option value="Department3">Program</option>
                </select>
            </div>
            <button type="submit">Save</button>
        </form>
    </div>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var form = document.getElementById("proracunForm");

            form.addEventListener("submit", function (event) {
                event.preventDefault();

                var allowedBudget = document.getElementById("allowedBudget").value;
                var month = document.getElementById("month").value;
                var department = document.getElementById("department").options[document.getElementById("department").selectedIndex].text;

                if (!/^\d+$/.test(allowedBudget)) {
                    alert("Allowed budget must be a number.");
                    return;
                }

                var tableBody = document.querySelector("table tbody");

                var rows = tableBody.querySelectorAll("tr");
                for (var i = 0; i < rows.length; i++) {
                    var rowMonth = rows[i].getElementsByTagName("td")[2].textContent;
                    if (rowMonth === month) {
                        alert("Budget for this month already exists.");
                        return;
                    }
                }

                var newRow = document.createElement("tr");
                newRow.innerHTML = "<td>" + (rows.length + 1) + "</td><td>" + allowedBudget + "</td><td>" + month + "</td><td>" + department + "</td>";

                tableBody.insertBefore(newRow, tableBody.firstChild);

                form.reset();
            });
        });
    </script>
</body>
</html>